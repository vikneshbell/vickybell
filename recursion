#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d",&a);
    printf("%d",sum(a));
    return 0;
}
 int sum(int a)
 {
     if(a==0)
     {
         return a;
     }
     else
     {
         return a+sum(a-1);
     }
 }

#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    printf("%d",sum(a));
    return 0;
}
 int sum(int a)
 {
     if(a==1)
     {
         return a;
     }
     else
     {
         return a*sum(a-1);
     }
 }
